import Layout from '@/components/layout/Layout';

export default function Markets() {
  // const { data, error } = useSWR(
  //   `/api/${process.env.NEXT_PUBLIC_LOCAL_API_VERSION}/coins/markets`
  // );

  return (
    <Layout>
      <main>
        <section className='bg-white'></section>
      </main>
    </Layout>
  );
}
